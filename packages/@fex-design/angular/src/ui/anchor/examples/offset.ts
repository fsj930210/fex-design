import { ChangeDetectionStrategy, Component, ElementRef, viewChild } from '@angular/core'; import { Anchor } from '../anchor'
@Component({selector:'anchor-ui-offset-example',standalone:true,imports:[Anchor],changeDetection:ChangeDetectionStrategy.OnPush,templateUrl:'./offset.html'}) export class AnchorUiOffsetExample{protected readonly items=[{key:'global',title:'全局偏移 48px',target:'#angular-ui-offset-global'},{key:'item',title:'单项覆盖 96px',target:'#angular-ui-offset-item',targetOffset:96}];private readonly containerRef=viewChild.required<ElementRef<HTMLElement>>('container');protected readonly container=()=>this.containerRef().nativeElement}
