import { SelectContent, SelectItem, SelectRoot, SelectTrigger } from '@fex-design/react/primitive/select'; const options=[{value:'react',label:'React'},{value:'vue',label:'Vue'}]; export default function Example(){return <SelectRoot options={options} defaultValue="react" clearable><SelectTrigger className="w-72"/><SelectContent>{options.map(option=><SelectItem key={option.value} value={option.value}>{option.label}</SelectItem>)}</SelectContent></SelectRoot>}
