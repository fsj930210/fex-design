<script setup lang="ts">
import { Bubble, BubbleContent } from '@fex-design/vue/primitive/bubble'
import Card from '@fex-design/vue/ui/card'
const variants = ['solid', 'soft', 'outline', 'plain', 'danger'] as const
</script>
<template>
  <Card
    title="Variants and rich content"
    description="Visual treatment stays independent from sender role and content remains fully composable."
    ><div class="grid gap-3">
      <Bubble v-for="variant in variants" :key="variant" :variant="variant"
        ><BubbleContent
          ><strong>{{ variant }}</strong> — arbitrary text, code, links, or custom
          content.</BubbleContent
        ></Bubble
      >
    </div></Card
  >
</template>
