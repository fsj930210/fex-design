# Drawer Primitive

Composable four-direction Drawer with optional mask, custom Header/Body/Footer, nested overlays, preset or CSS sizes, and Resize composable integration. Accessibility naming is supplied through native `aria-label` or `aria-labelledby` on `DrawerContent`.
