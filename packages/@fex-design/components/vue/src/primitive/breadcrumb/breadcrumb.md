# Breadcrumb

Vue Breadcrumb primitives render a semantic `nav` and `ol` structure. Use `BreadcrumbLink` for navigable paths, `BreadcrumbPage` for the current or non-navigable path, and replace `BreadcrumbSeparator` slot content when needed. Routing and dropdown behavior remain caller-owned and can be composed with the existing Dropdown primitive.
