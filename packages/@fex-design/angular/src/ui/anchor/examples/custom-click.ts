import { ChangeDetectionStrategy, Component, ElementRef, viewChild } from '@angular/core'; import { Anchor } from '../anchor'
@Component({selector:'anchor-ui-custom-click-example',standalone:true,imports:[Anchor],changeDetection:ChangeDetectionStrategy.OnPush,templateUrl:'./custom-click.html'}) export class AnchorUiCustomClickExample{protected readonly items=[{key:'normal',title:'正常点击并滚动',target:'#angular-ui-click-normal'},{key:'blocked',title:'阻止内部滚动',target:'#angular-ui-click-blocked'}];private readonly containerRef=viewChild.required<ElementRef<HTMLElement>>('container');protected readonly container=()=>this.containerRef().nativeElement;protected click({event,item}:{event:MouseEvent;item:{key:string}}){console.log('点击锚点',item.key);if(item.key==='blocked')event.preventDefault()}}
