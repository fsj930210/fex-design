import { Anchor } from '@fex-design/solid/ui/anchor'; import { createSignal, For } from 'solid-js'; const items=(d:'ltr'|'rtl')=>[{key:`${d}-overview`,title:d==='rtl'?'نظرة عامة':'概览',target:`#solid-ui-${d}-overview`},{key:`${d}-usage`,title:d==='rtl'?'طريقة الاستخدام':'使用方式',target:`#solid-ui-${d}-usage`,children:[{key:`${d}-api`,title:d==='rtl'?'واجهة البرمجة':'API',target:`#solid-ui-${d}-api`}]}]
export default function Direction(){return <div class="grid w-full gap-6 sm:grid-cols-2"><For each={['ltr','rtl'] as const}>{d=>{const[c,setC]=createSignal<HTMLElement>();return <section ref={setC} dir={d} class="h-80 overflow-auto rounded-lg border"><div class="grid min-h-full grid-cols-[8rem_minmax(0,1fr)] gap-6 p-4"><div class="sticky top-4 self-start"><strong>{d==='rtl'?'RTL · مثال عربي':'LTR · 中文示例'}</strong><Anchor items={items(d)} container={()=>c()!} class="mt-3"/></div><div class="grid gap-32 pb-64"><section id={`solid-ui-${d}-overview`}>{d==='rtl'?'نظرة عامة':'概览'}</section><section id={`solid-ui-${d}-usage`}>{d==='rtl'?'طريقة الاستخدام':'使用方式'}</section><section id={`solid-ui-${d}-api`}>{d==='rtl'?'واجهة البرمجة':'API'}</section></div></div></section>}}</For></div>}
