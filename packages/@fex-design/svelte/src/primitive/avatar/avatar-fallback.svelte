<script lang="ts">import { avatarFallbackClassName } from '@fex-design/styles/avatar';import { cn } from '@fex/utils';import { getContext,type Snippet } from 'svelte';import type { HTMLAttributes } from 'svelte/elements';import { avatarContextKey,type AvatarContext } from './context';interface Props extends Omit<HTMLAttributes<HTMLSpanElement>,'class'>{class?:string;children?:Snippet}let{class:className,children,...rest}:Props=$props();const{status}=getContext<AvatarContext>(avatarContextKey)</script>
{#if $status !== 'loaded'}<span {...rest} data-slot="avatar-fallback" class={cn(avatarFallbackClassName,className)}>{@render children?.()}</span>{/if}
