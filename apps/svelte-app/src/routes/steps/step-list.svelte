<script lang="ts">
  import Step from "@fex-design/svelte/primitive/step";
  import Content from "@fex-design/svelte/primitive/step-content";
  import Indicator from "@fex-design/svelte/primitive/step-indicator";
  let {
    error = false,
    disabled = false,
  }: { error?: boolean; disabled?: boolean } = $props();
  const items = [
    ["account", "Account", "Create your workspace"],
    ["profile", "Profile", "Add personal details"],
    ["review", "Review", "Confirm and submit"],
  ] as const;
</script>

{#each items as item (item[0])}<Step
    value={item[0]}
    disabled={disabled && item[0] === "profile"}
    status={error && item[0] === "profile" ? "error" : undefined}
    data={item[1]}
    ><Indicator /><Content
      ><strong>{item[1]}</strong>
      <p class="text-muted-foreground">{item[2]}</p></Content
    ></Step
  >{/each}
