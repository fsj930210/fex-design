<script lang="ts">
  import { Bubble, BubbleContent } from "@fex-design/svelte/primitive/bubble";
  import Button from "@fex-design/svelte/ui/button";
  import Card from "@fex-design/svelte/ui/card";
</script>

{#snippet buttonContent({ props }: { props: Record<string, unknown> })}<Button
    class={String(props.class ?? "")}
    variant="secondary">I forgot my password</Button
  >{/snippet}{#snippet linkContent({
  props,
}: {
  props: Record<string, unknown>;
})}<a {...props} href="#subscription">I need help with my subscription</a
  >{/snippet}<Card
  title="Links and buttons"
  description="Callers choose the actual interactive element."
  ><div class="grid gap-2">
    <Bubble><BubbleContent render={buttonContent} /></Bubble><Bubble
      ><BubbleContent render={linkContent} /></Bubble
    >
  </div></Card
>
