---
title: Select
description: 从动态数据中选择一个或多个已有项目。
---

## 何时使用

Select 用于从已有选项中选择一个或多个值。需要自由输入新值时使用 TagsInput；需要输入建议但不限制最终值时使用 AutoComplete。

## 示例

<Demos />

## API

<ApiContainer>
<ApiSection layer="ui" title="UI API" description="通过 options 快速创建完整 Select。">
  <ApiComposition layer="ui">
```text
Select
├── Input Trigger
└── Popup List
```
  </ApiComposition>
</ApiSection>

<ApiSection layer="primitive" title="Primitive API" description="可组合 Select 部件与共享状态。">
  <ApiComposition layer="primitive">

```text
SelectRoot
├── SelectTrigger
│   └── SelectValue
└── SelectContent
    ├── SelectGroup
    │   ├── SelectLabel
    │   └── SelectItem ...
    ├── SelectSeparator
    └── SelectItem ...
```

  </ApiComposition>
</ApiSection>
</ApiContainer>

## CSS Variables

Select 的 Trigger 使用 Input 变量，面板使用 Popover/Floating 变量；虚拟行高由 `virtual.itemHeight` 控制。

<CssVariables />
