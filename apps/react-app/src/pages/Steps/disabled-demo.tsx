import { useState } from 'react'; import { Steps, type StepValue } from '@fex-design/react/primitive/steps'; import { Card } from '@fex-design/react/ui/card'; import { StepList } from './step-list'
export function DisabledDemo(){const [value,setValue]=useState<StepValue>('account');return <Card title="Disabled" description="Disabled nodes cannot be selected and are skipped by keyboard navigation."><Steps navigation current={value} onChange={setValue}><StepList disabled/></Steps><p className="mt-2 text-sm text-muted-foreground">Current value: {String(value)}</p></Card>}
