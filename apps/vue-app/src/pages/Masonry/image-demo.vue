<script setup lang="ts">
import { MasonryItem, MasonryRoot, MasonryViewport } from '@fex-design/vue/primitive/masonry'
const images = [
  [10, 4, 3],
  [20, 3, 4],
  [40, 1, 1],
  [30, 16, 9],
  [60, 4, 5],
  [50, 5, 4],
  [70, 3, 2],
]
</script>
<template>
  <MasonryRoot :columns="{ minColumnWidth: 160, max: 3 }" :gap="16"
    ><MasonryViewport
      ><MasonryItem
        v-for="([id, width, height], index) in images"
        :key="id"
        :item-key="id!"
        :index="index"
        ><article class="overflow-hidden rounded-md border border-border bg-muted-background">
          <div
            class="relative overflow-hidden bg-muted-background"
            :style="{ aspectRatio: `${width}/${height}` }"
          >
            <img
              class="absolute inset-0 h-full w-full object-cover"
              :src="`https://picsum.photos/id/${id}/640/${Math.round((640 * height!) / width!)}`"
              :alt="`瀑布流示例图片 ${index + 1}`"
              @error="
                (event) => {
                  ;(event.currentTarget as HTMLImageElement).hidden = true
                }
              "
            />
          </div>
          <p class="p-1.5 text-sm">比例 {{ width }}:{{ height }}</p>
        </article></MasonryItem
      ></MasonryViewport
    ></MasonryRoot
  >
</template>
