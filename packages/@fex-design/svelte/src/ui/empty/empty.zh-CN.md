# Svelte UI Empty

使用 Svelte 原生字符串和 Snippet 的快捷 Empty。

## 引入

    import Empty from '@fex-design/svelte/ui/empty'

`image`、`title`、`description` 接受字符串或 Snippet，`children` 渲染到 EmptyContent；`classNames`、`styles` 定制六个语义区域。四个公共示例与 Primitive 展示一致，`styling` 为 UI 专属。

