# Anchor

Anchor tracks target elements in a window or custom scroll container. `activeKeys` is always an array: `current` contains at most one key, while `progress` contains every item passed so far. The primitive uses buttons and never changes the URL. Vertical anchors support real nested headings; horizontal anchors render the first level only. Use `useAnchor` to connect the behavior to custom React UI.
