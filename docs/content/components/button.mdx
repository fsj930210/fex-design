---
title: Button
description: 触发即时操作，并清晰表达动作层级与当前状态。
---

## Primitive 与 UI

Primitive 提供原生按钮、图标容器和按钮组。

UI 在此基础上增加尺寸、图标、加载状态、交互效果和预设外观。

## 何时使用

Button 用于触发即时操作。页面跳转使用链接；开关状态使用 Toggle；互斥选择使用 ToggleGroup、Radio 或 Tabs。

<Layer value="primitive">

## Primitive 能力

`Button`、`ButtonIcon` 和 `ButtonGroup` 可独立组合使用。

</Layer>

## 示例

示例展示组件的常用用法。

<Demos />

## API

<ApiSection
  layer="primitive"
  title="Primitive API"
  description="Primitive Button、ButtonIcon 与 ButtonGroup 的公开属性。"
>
  <ApiComposition layer="primitive">
    Primitive `ButtonIcon` 用于图标内容，`ButtonGroup` 用于组织相关按钮。

    ```text
    Button
    └── ButtonIcon (optional)

    ButtonGroup
    ├── Button
    └── Button
    ```

  </ApiComposition>
</ApiSection>

<ApiSection layer="ui" title="UI API" description="UI Button 的公开属性。">
  <ApiComposition layer="ui">
    UI `Button` 通过 icon API 或框架原生内容入口接收图标，不公开 `ButtonIcon`；`ButtonGroup` 直接复用 Primitive。

    ```text
    Button

    ButtonGroup
    ├── Button
    └── Button
    ```

  </ApiComposition>
</ApiSection>

## CSS Variables

Button 默认从全局 `--height-sm`、`--height-md`、`--height-lg` 取得高度，md 图标从 `--icon-size-md` 取得尺寸。只调整 Button 时使用 `--button-height-*` 和 `--button-icon-size-*`；单个实例可继续用 `--button-height`、`--button-icon-size` 覆盖最终结果。变量可写在组件、局部容器或主题根节点上。

<CssVariables />
