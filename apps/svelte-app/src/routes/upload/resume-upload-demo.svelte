<script lang="ts">import { UploadRoot, UploadTrigger } from '@fex-design/svelte/primitive/upload'; import { Button } from '@fex-design/svelte/ui/button'; import { createMultipartDemoUpload } from './create-multipart-upload'; import DemoList from './demo-list.svelte'; import DemoSection from './demo-section.svelte'; const upload = createMultipartDemoUpload('resume')</script>
<DemoSection title="断点续传" description="暂停后可直接继续；刷新页面后重新选择同一个文件，也会根据 MD5 找回 uploadId，查询服务端已完成分片并跳过它们。浏览器安全限制决定了刷新后必须由用户重新选择文件。"><UploadRoot controller={upload}><UploadTrigger>{#snippet children(value)}<Button {...value.props}>选择需要续传的文件</Button>{/snippet}</UploadTrigger><DemoList showMultipart /></UploadRoot></DemoSection>
