# Breadcrumb

Angular Breadcrumb primitives use standalone components and host classes. Apply `fexBreadcrumbLink` to an anchor, use `fex-breadcrumb-page` for the current path, and compose separators or the existing Dropdown directive as needed. The primitive does not own router navigation or popup state.
