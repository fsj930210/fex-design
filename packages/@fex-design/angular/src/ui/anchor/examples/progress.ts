import { ChangeDetectionStrategy, Component, ElementRef, viewChild } from '@angular/core'; import { Anchor } from '../anchor'
@Component({selector:'anchor-ui-progress-example',standalone:true,imports:[Anchor],changeDetection:ChangeDetectionStrategy.OnPush,templateUrl:'./progress.html'}) export class AnchorUiProgressExample{protected readonly items=[['prepare','准备工作'],['install','安装'],['configure','配置'],['finish','完成']].map(([key,title])=>({key:key!,title:title!,target:`#angular-ui-progress-${key}`}));private readonly containerRef=viewChild.required<ElementRef<HTMLElement>>('container');protected readonly container=()=>this.containerRef().nativeElement}
