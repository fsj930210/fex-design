# Breadcrumb

The Svelte Breadcrumb primitive uses the `part` prop to render root, list, item, link, page, separator, or ellipsis parts. It owns structure and classes only; routing, menus, and popup state are composed by the caller.
