---
title: Anchor
description: 跟随页面滚动高亮当前内容位置，不依赖 hash，也不会修改 URL。
---

<ManualInstall />

## 何时使用

用于长页面、文档或局部滚动容器的内容导航。`target` 可以是选择器、元素、ref-like 对象或解析函数；组件只查找并滚动到目标，不写入 URL。

## 示例

<Demos />

## API

<ApiContainer>
<ApiSection layer="ui" title="UI API" description="开箱即用的 Anchor 高阶配置属性。" />

<ApiSection
  layer="primitive"
  title="Primitive API"
  description="解构导出的 Anchor 子部件结构与原生插槽。"
>
  <ApiComposition layer="primitive">

```text
AnchorRoot
├── AnchorRail
│   └── AnchorIndicator
└── AnchorList
    └── AnchorItem
        ├── AnchorLink
        └── AnchorList (optional)
```

  </ApiComposition>
</ApiSection>
</ApiContainer>
