# Message

Compositional layout for one conversation row. `Message` owns side and busy state; compose `MessageAvatar`, `MessageBody`, `MessageHeader`, `MessageContent`, `MessageStatus`, `MessageActions`/`MessageAction`, and `MessageFooter`. Content may use Bubble or any custom component. `MessageGroup` arranges consecutive rows.
