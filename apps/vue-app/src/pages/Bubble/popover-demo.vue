<script setup lang="ts">
import { Bubble, BubbleContent } from '@fex-design/vue/primitive/bubble'
import {
  PopoverArrow,
  PopoverContent,
  PopoverDescription,
  PopoverHeader,
  PopoverPortal,
  PopoverRoot,
  PopoverTitle,
  PopoverTrigger,
} from '@fex-design/vue/primitive/popover'
import Card from '@fex-design/vue/ui/card'
</script>
<template>
  <Card title="Popover" description="Compose existing Popover for error details."
    ><PopoverRoot placement="bottomLeft" arrow
      ><PopoverTrigger v-slot="trigger"
        ><Bubble variant="danger"
          ><BubbleContent
            ><template #render="{ props }"
              ><button v-bind="{ ...props, ...trigger.props }" :ref="trigger.ref">
                Failed to run the command. Show details.
              </button></template
            ></BubbleContent
          ></Bubble
        ></PopoverTrigger
      ><PopoverPortal
        ><PopoverContent
          ><PopoverArrow /><PopoverHeader class="w-72"
            ><PopoverTitle>Command error</PopoverTitle
            ><PopoverDescription
              >Dependency installation exited with code 1.</PopoverDescription
            ></PopoverHeader
          ></PopoverContent
        ></PopoverPortal
      ></PopoverRoot
    ></Card
  >
</template>
