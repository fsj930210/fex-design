---
title: Switch
description: 用于立即切换两个互斥状态。
---

## Primitive 与 UI

Primitive 提供 SwitchRoot、SwitchContent 和 SwitchThumb，可按框架原生方式自由组合。UI 提供一个快捷 Switch，接收 checkedContent、uncheckedContent，并在 loading 时提供默认加载图标。

## 示例

Primitive 与 UI 示例统一按基本展示、Shape、Size、文本和图标、Loading、Disabled、校验失败、受控与非受控、LTR 与 RTL、CSS Variables 排列；每个场景同时展示 rounded 与 pill。UI 另有结构化样式示例，通过 `classNames` 和 `styles` 定制 `root`、`content`、`thumb`。

<Demos />

## API

<ApiSection
  layer="primitive"
  title="Primitive API"
  description="SwitchRoot、SwitchContent 与 SwitchThumb 的组件 API。"
/>
<ApiSection layer="ui" title="UI API" description="快捷组合 Switch 的组件 API。" />

## CSS Variables

| 变量                                                               | 用途                                                               |
| ------------------------------------------------------------------ | ------------------------------------------------------------------ |
| `--switch-height-sm` / `--switch-height-md` / `--switch-height-lg` | Switch 各尺寸的组件级高度；未设置时回退到对应的全局 `--height-*`。 |
| `--switch-track-height`                                            | 当前尺寸的轨道高度。                                               |
| `--switch-track-background`                                        | 关闭状态的轨道背景。                                               |
| `--switch-track-checked-background`                                | 开启状态的轨道背景。                                               |
| `--switch-track-border-color`                                      | 关闭状态的轨道边框。                                               |
| `--switch-track-checked-border-color`                              | 开启状态的轨道边框。                                               |
| `--switch-color`                                                   | 关闭状态的内容颜色。                                               |
| `--switch-checked-color`                                           | 开启状态的内容颜色。                                               |
| `--switch-thumb-background`                                        | 滑块背景。                                                         |
| `--switch-thumb-color`                                             | 滑块内容颜色。                                                     |
| `--switch-thumb-size`                                              | 当前尺寸的滑块边长。                                               |
| `--switch-thumb-radius`                                            | 当前形状的滑块圆角。                                               |
