# Button

Button 用于触发即时操作。一个区域应只有一个明确的主要操作，其余动作使用次级或弱化层级。

## 基础用法

::demo{#button-basic}

在属性区切换 `variant`、`size`、`loading` 与 `disabled`，点击按钮后可在 Events JSON 中观察回调参数。

## API

::api{component="button" layer="ui"}
