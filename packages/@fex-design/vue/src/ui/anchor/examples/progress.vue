<script setup lang="ts">import { ref } from 'vue'; import { Anchor } from '@fex-design/vue/ui/anchor'; const container=ref<HTMLElement>(); const items=[['prepare','准备工作'],['install','安装'],['configure','配置'],['finish','完成']].map(([key,title])=>({key:key!,title:title!,target:`#vue-ui-progress-${key}`}))</script>
<template><div ref="container" class="grid h-80 w-full max-w-2xl grid-cols-[10rem_1fr] gap-6 overflow-auto rounded-lg border p-4"><Anchor :items="items" active-mode="progress" :container="() => container!" class="sticky top-0 self-start"/><div class="grid gap-24 pb-48"><section v-for="item in items" :id="`vue-ui-progress-${item.key}`" :key="item.key"><h3 class="font-semibold">{{ item.title }}</h3></section></div></div></template>
