import { Anchor } from '@fex-design/solid/ui/anchor'; import { createSignal,For } from 'solid-js'; const items=[['prepare','准备工作'],['install','安装'],['configure','配置'],['finish','完成']].map(([key,title])=>({key:key!,title:title!,target:`#solid-ui-progress-${key}`}))
export default function Progress(){const[c,setC]=createSignal<HTMLDivElement>();return <div ref={setC} class="grid h-80 w-full max-w-2xl grid-cols-[10rem_1fr] gap-6 overflow-auto rounded-lg border p-4"><Anchor items={items} activeMode="progress" container={()=>c()!} class="sticky top-0 self-start"/><div class="grid gap-24 pb-48"><For each={items}>{item=><section id={`solid-ui-progress-${item.key}`}><h3 class="font-semibold">{item.title}</h3></section>}</For></div></div>}
