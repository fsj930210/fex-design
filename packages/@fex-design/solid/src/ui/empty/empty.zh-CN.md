# Solid UI Empty

由 Primitive 区域组成的快捷 Empty。

## 引入

    import { Empty } from '@fex-design/solid/ui/empty'

`image`、`title`、`description` 和 `children` 提供内容；`classNames`、`styles` 定制六个语义区域，原生 div 属性继续透传。`basic`、`content`、`image`、`direction` 与 Primitive 展示一致；`styling` 为 UI 专属。

