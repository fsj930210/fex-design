# Vue Primitive Checkbox

Styled, composable Checkbox parts. CheckboxControl always renders a native input[type=checkbox].

## API

CheckboxRoot owns layout and association. CheckboxControl inherits native input attributes and only adds indeterminate. CheckboxIndicator contains marks. CheckboxLabel is a native label. CheckboxGroup manages a flat value array.

Primitive and UI share the same examples and CSS variables.
