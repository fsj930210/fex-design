---
title: Button
description: 触发即时操作，并清晰表达动作层级与当前状态。
---

## 何时使用

Button 用于触发即时操作。页面跳转使用链接；开关状态使用 Toggle；互斥选择使用 ToggleGroup、Radio 或 Tabs。

## 示例

<Demos />

## API

<ApiContainer>
<ApiSection
  layer="ui"
  title="UI API"
  description="开箱即用的高阶 Button 组件配置属性。"
>
  <ApiComposition layer="ui">
    ```text
    Button

    ButtonGroup
    ├── Button
    └── Button
    ```
  </ApiComposition>
</ApiSection>

<ApiSection
  layer="primitive"
  title="Primitive API"
  description="解构导出的 Primitive Button、ButtonIcon 与 ButtonGroup 部件。"
>
  <ApiComposition layer="primitive">
    ```text
    Button
    └── ButtonIcon (optional)

    ButtonGroup
    ├── Button
    └── Button
    ```
  </ApiComposition>
</ApiSection>
</ApiContainer>

## CSS Variables

Button 默认从全局 `--height-sm`、`--height-md`、`--height-lg` 取得高度，md 图标从 `--icon-size-md` 取得尺寸。只调整 Button 时使用 `--button-height-*` 和 `--button-icon-size-*`；单个实例可继续用 `--button-height`、`--button-icon-size` 覆盖最终结果。变量可写在组件、局部容器或主题根节点上。

<CssVariables />
