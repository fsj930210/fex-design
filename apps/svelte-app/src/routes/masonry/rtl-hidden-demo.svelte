<script lang="ts">import { MasonryItem,MasonryRoot,MasonryViewport } from '@fex-design/svelte/primitive/masonry';import Button from '@fex-design/svelte/ui/button';import { masonryItems } from './data';let rtl=$state(false),visible=$state(true)</script>
<div class="grid gap-2"><div class="flex gap-1.5"><Button size="sm" variant="outline" onclick={()=>rtl=!rtl}>方向：{rtl?'RTL':'LTR'}</Button><Button size="sm" variant="outline" onclick={()=>visible=!visible}>{visible?'隐藏':'显示'}容器</Button></div>{#if visible}<MasonryRoot columns={3} gap={12} direction={rtl?'rtl':'ltr'}><MasonryViewport>{#each masonryItems.slice(0,8) as item,index(item.id)}<MasonryItem itemKey={item.id} {index}><div class="rounded-md border border-border bg-muted-background p-1.5" style:height={`${item.height/2}px`}>DOM {index+1} · {rtl?'RTL':'LTR'}</div></MasonryItem>{/each}</MasonryViewport></MasonryRoot>{/if}</div>
