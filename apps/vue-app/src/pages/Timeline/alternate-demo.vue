<script setup lang="ts">
import {
  Timeline,
  TimelineContent,
  TimelineIndicator,
  TimelineItem,
  TimelineOpposite,
} from '@fex-design/vue/primitive/timeline'
import Card from '@fex-design/vue/ui/card'
const labels = ['Project created', 'Design approved', 'Implementation finished', 'Release']
</script>
<template>
  <Card
    title="Alternate"
    description="Items automatically alternate around a centered vertical axis."
    ><Timeline align="alternate"
      ><TimelineItem
        v-for="(label, index) in labels"
        :key="label"
        :status="index < 3 ? 'completed' : 'current'"
        ><TimelineOpposite>Day {{ index + 1 }}</TimelineOpposite
        ><TimelineIndicator /><TimelineContent>{{ label }}</TimelineContent></TimelineItem
      ></Timeline
    ></Card
  >
</template>
