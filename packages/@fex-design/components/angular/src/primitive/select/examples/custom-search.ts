import { ChangeDetectionStrategy, Component } from '@angular/core'; import type { SelectOption } from '@fex-design/core/select/types'; import { SelectContent,SelectRoot,SelectTrigger } from '@fex-design/angular/primitive/select'; @Component({selector:'select-custom-example',imports:[SelectRoot,SelectTrigger,SelectContent],changeDetection:ChangeDetectionStrategy.OnPush,templateUrl:'./custom-search.html'}) export class SelectCustomExample { readonly options=[{value:'react',label:'React'},{value:'vue',label:'Vue'},{value:'solid',label:'Solid'}]; readonly filter=(keyword:string,option:SelectOption)=>{let from=0;return [...keyword.toLowerCase()].every(character=>{const index=option.label.toLowerCase().indexOf(character,from);from=index+1;return index>=0})} }
