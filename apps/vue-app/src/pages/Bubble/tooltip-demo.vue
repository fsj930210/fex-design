<script setup lang="ts">
import { Bubble, BubbleContent } from '@fex-design/vue/primitive/bubble'
import {
  TooltipArrow,
  TooltipContent,
  TooltipPortal,
  TooltipRoot,
  TooltipTrigger,
} from '@fex-design/vue/primitive/tooltip'
import Card from '@fex-design/vue/ui/card'
</script>
<template>
  <Card title="Tooltip" description="Compose the existing Tooltip to reveal delivery metadata."
    ><div class="mx-auto grid w-full max-w-2xl gap-4 py-8">
      <Bubble
        ><BubbleContent
          >Did you remove the stale route and regenerate the registry?</BubbleContent
        ></Bubble
      ><TooltipRoot
        ><TooltipTrigger v-slot="trigger"
          ><Bubble side="end" variant="solid"
            ><BubbleContent
              ><template #render="{ props }"
                ><button v-bind="{ ...props, ...trigger.props }" :ref="trigger.ref">
                  Yes. The route is gone and the registry is clean.
                </button></template
              ></BubbleContent
            ></Bubble
          ></TooltipTrigger
        ><TooltipPortal
          ><TooltipContent
            >Read at 10:24 · Delivered to 3 devices<TooltipArrow /></TooltipContent></TooltipPortal></TooltipRoot
      ><Bubble variant="plain"
        ><BubbleContent class="text-muted-foreground"
          >Hover or focus the reply to inspect its delivery metadata.</BubbleContent
        ></Bubble
      >
    </div></Card
  >
</template>
