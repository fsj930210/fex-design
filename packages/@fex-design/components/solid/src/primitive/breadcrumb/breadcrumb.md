# Breadcrumb

Solid Breadcrumb primitives provide semantic navigation structure. Links render anchors and pages render non-clickable current content. Separators and ellipsis accept children so callers can compose custom icons, shapes, and the existing Dropdown primitive without coupling routing to Breadcrumb.
