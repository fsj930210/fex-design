# TreeSelect

TreeSelect connects Input, Popover, Tree, and value state only. Applications own synchronous or
remote search and may render a Tree, Listbox, or custom result panel. For async results, returning a
structured ancestor path helps disambiguate duplicate labels. For multiple selection, checkbox
feedback is a useful default because selections remain visible across keywords, but it is not a
component requirement.
