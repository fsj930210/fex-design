import { ChangeDetectionStrategy, Component, signal } from '@angular/core'; import { Anchor } from '../anchor'
@Component({selector:'anchor-ui-controlled-example',standalone:true,imports:[Anchor],changeDetection:ChangeDetectionStrategy.OnPush,templateUrl:'./controlled.html'}) export class AnchorUiControlledExample{protected readonly activeKeys=signal<readonly string[]>(['controlled-second']);protected readonly uncontrolled=[{key:'uncontrolled-first',title:'第一项',target:'#angular-ui-uncontrolled-first'},{key:'uncontrolled-second',title:'第二项',target:'#angular-ui-uncontrolled-second'}];protected readonly controlled=[{key:'controlled-first',title:'第一项',target:'#angular-ui-controlled-first'},{key:'controlled-second',title:'第二项',target:'#angular-ui-controlled-second'}];protected uncontrolledChange(event:any){console.log('uncontrolled change',event.activeKeys,event.items)}protected controlledChange(event:any){this.activeKeys.set(event.activeKeys);console.log('controlled change',event.activeKeys,event.items)}}
