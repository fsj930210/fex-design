<script lang="ts">import { Anchor } from '@fex-design/svelte/ui/anchor'; let container:HTMLDivElement; const items=[['prepare','准备工作'],['install','安装'],['configure','配置'],['finish','完成']].map(([key,title])=>({key:key!,title:title!,target:`#svelte-ui-progress-${key}`}))</script>
<div bind:this={container} class="grid h-80 w-full max-w-2xl grid-cols-[10rem_1fr] gap-6 overflow-auto rounded-lg border p-4"><Anchor {items} activeMode="progress" container={()=>container} class="sticky top-0 self-start"/><div class="grid gap-24 pb-48">{#each items as item}<section id={`svelte-ui-progress-${item.key}`}><h3 class="font-semibold">{item.title}</h3></section>{/each}</div></div>
