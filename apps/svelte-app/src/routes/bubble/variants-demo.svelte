<script lang="ts">
  import { Bubble, BubbleContent } from "@fex-design/svelte/primitive/bubble";
  import Card from "@fex-design/svelte/ui/card";
  const variants = ["solid", "soft", "outline", "plain", "danger"] as const;
</script>

<Card
  title="Variants and rich content"
  description="Visual treatment stays independent from sender role and content remains composable."
  ><div class="grid gap-3">
    {#each variants as variant (variant)}<Bubble {variant}
        ><BubbleContent
          ><strong>{variant}</strong> — arbitrary text, code, links, or custom content.</BubbleContent
        ></Bubble
      >{/each}
  </div></Card
>
