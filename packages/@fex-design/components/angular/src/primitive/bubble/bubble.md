# Bubble

Styled compositional conversation surface with side inheritance, groups, actions, and reactions. `fexBubbleAction` and `fexBubbleReaction` attach Toggle behavior and styles to the caller's chosen host element.
