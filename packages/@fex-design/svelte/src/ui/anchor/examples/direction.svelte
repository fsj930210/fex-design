<script lang="ts">import { Anchor } from '@fex-design/svelte/ui/anchor'; let containers:Record<string,HTMLElement>={}; const directions=['ltr','rtl'] as const; const items=(d:'ltr'|'rtl')=>[{key:`${d}-overview`,title:d==='rtl'?'نظرة عامة':'概览',target:`#svelte-ui-${d}-overview`},{key:`${d}-usage`,title:d==='rtl'?'طريقة الاستخدام':'使用方式',target:`#svelte-ui-${d}-usage`,children:[{key:`${d}-api`,title:d==='rtl'?'واجهة البرمجة':'API',target:`#svelte-ui-${d}-api`}]}]</script>
<div class="grid w-full gap-6 sm:grid-cols-2">{#each directions as direction}<section bind:this={containers[direction]} dir={direction} class="h-80 overflow-auto rounded-lg border"><div class="grid min-h-full grid-cols-[8rem_minmax(0,1fr)] gap-6 p-4"><div class="sticky top-4 self-start"><strong>{direction==='rtl'?'RTL · مثال عربي':'LTR · 中文示例'}</strong><Anchor items={items(direction)} container={()=>containers[direction]} class="mt-3"/></div><div class="grid gap-32 pb-64"><section id={`svelte-ui-${direction}-overview`}>{direction==='rtl'?'نظرة عامة':'概览'}</section><section id={`svelte-ui-${direction}-usage`}>{direction==='rtl'?'طريقة الاستخدام':'使用方式'}</section><section id={`svelte-ui-${direction}-api`}>{direction==='rtl'?'واجهة البرمجة':'API'}</section></div></div></section>{/each}</div>
