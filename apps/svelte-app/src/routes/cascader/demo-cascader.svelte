<script lang="ts">
  import type {
    CascaderFieldNames,
    CascaderFilterOption,
    CascaderOption,
    CascaderValue,
  } from "@fex-design/core/cascader/types";
  import CascaderRoot from "@fex-design/svelte/primitive/cascader";
  import CascaderContent from "@fex-design/svelte/primitive/cascader-content";
  import CascaderPanel from "@fex-design/svelte/primitive/cascader-panel";
  import CascaderTrigger from "@fex-design/svelte/primitive/cascader-trigger";
  let {
    options = [],
    fieldNames,
    value,
    defaultValue,
    onChange,
    multiple = false,
    checkStrictly = false,
    changeOnSelect = false,
    expandTrigger = "click",
    showSearch = false,
    filterOption,
    onSearch,
    loadData,
    loading = false,
    status,
    triggerProps = {},
  }: {
    options?: readonly CascaderOption[];
    fieldNames?: CascaderFieldNames;
    value?: CascaderValue;
    defaultValue?: CascaderValue;
    onChange?: (value: CascaderValue) => void;
    multiple?: boolean;
    checkStrictly?: boolean;
    changeOnSelect?: boolean;
    expandTrigger?: "click" | "hover";
    showSearch?: boolean;
    filterOption?: boolean | CascaderFilterOption;
    onSearch?: (keyword: string) => void;
    loadData?: (path: readonly CascaderOption[]) => Promise<void>;
    loading?: boolean;
    status?: "error" | "warning";
    triggerProps?: Record<string, unknown>;
  } = $props();
</script>

<CascaderRoot
  {options}
  {fieldNames}
  {value}
  {defaultValue}
  {multiple}
  {checkStrictly}
  {changeOnSelect}
  {expandTrigger}
  {showSearch}
  {filterOption}
  {onSearch}
  {loadData}
  {loading}
  {status}
  clearable
  placeholder="请选择地区"
  onChange={(next) => onChange?.(next)}
  ><CascaderTrigger {...triggerProps} /><CascaderContent
    ><CascaderPanel /></CascaderContent
  ></CascaderRoot
>
