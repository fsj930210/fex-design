# ColorPicker

`ColorPickerRoot` manages color state while `Area`, `Channel`, `Swatch` and gradient parts remain independently composable. Supported formats are HEX, RGB, HSL, HSB and OKLCH. Overlay triggers, inputs, presets, clearing and validation are composed by consumers.
