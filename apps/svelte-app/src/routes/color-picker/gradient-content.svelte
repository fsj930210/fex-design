<script lang="ts">
  import {
    GradientPickerStop,
    GradientPickerTrack,
    useGradientPicker,
  } from "@fex-design/svelte/primitive/color-picker";
  const picker = useGradientPicker(),
    snapshot = $derived(picker.snapshot());
</script>

<GradientPickerTrack
  >{#each snapshot.value.stops as stop (stop.id)}<GradientPickerStop
      id={stop.id}
      aria-label={`${Math.round(stop.position * 100)}% color stop`}
    />{/each}</GradientPickerTrack
>
