# Bubble

Styled compositional conversation surface with side inheritance, groups, actions, and reactions. Action and reaction items default to Toggle and accept a render snippet for custom elements.
