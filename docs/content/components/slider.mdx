---
title: Slider
description: 在连续或离散区间中选择单值、范围或多个节点。
---

## Primitive 与 UI

Primitive 提供 SliderRoot、SliderTrack、SliderRange、SliderThumb 和 SliderMark，适合权重分配、双色轨道等组合场景。UI 提供自动组装的 Slider：number 表示单值，number[] 表示范围或多个节点。

## 何时使用

适合音量、比例、温度、价格范围、权重和阶段节点。精确数字输入应配合数值输入框；少量固定选项优先使用 Radio。

## 示例

Primitive 与 UI 示例统一覆盖基本使用、权重分配、范围与多节点、整体和单 Thumb 禁用、范围拖拽、动态增删节点、刻度与步长、LTR/RTL/Reverse/垂直、尺寸和 CSS Variables。UI 另有结构化样式示例。

<Demos />

## API

<ApiContainer>
<ApiSection layer="ui" title="UI API" description="自动组装 Slider 的快捷 API。" />

<ApiSection
  layer="primitive"
  title="Primitive API"
  description="SliderRoot、SliderTrack、SliderRange、SliderThumb 与 SliderMark。"
/>
</ApiContainer>

## CSS Variables

| 变量                          | 用途                         |
| ----------------------------- | ---------------------------- |
| `--slider-track-height`       | 水平轨道高度或垂直轨道宽度。 |
| `--slider-track-background`   | 未选中轨道背景。             |
| `--slider-range-background`   | 选中范围背景。               |
| `--slider-thumb-size`         | Thumb 尺寸。                 |
| `--slider-thumb-background`   | Thumb 背景。                 |
| `--slider-thumb-border-color` | Thumb 边框颜色。             |
