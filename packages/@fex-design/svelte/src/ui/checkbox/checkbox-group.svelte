<script lang="ts">import type{CheckboxValue}from"@fex-design/core/checkbox/types";import type{Snippet}from"svelte";import Checkbox from"./checkbox.svelte";import PrimitiveGroup from"../../primitive/checkbox/checkbox-group.svelte";interface Props{value?:CheckboxValue[];defaultValue?:CheckboxValue[];disabled?:boolean;orientation?:"horizontal"|"vertical";options?:{label:string;value:CheckboxValue;disabled?:boolean}[];onChange?:(value:CheckboxValue[],meta:unknown)=>void;children?:Snippet}let{options,children,...rest}:Props=$props()</script>
<PrimitiveGroup {...rest}>{#if options}{#each options as option (option.value)}<Checkbox value={option.value} disabled={option.disabled}>{option.label}</Checkbox>{/each}{:else}{@render children?.()}{/if}</PrimitiveGroup>
